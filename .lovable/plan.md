# Compact CHEESESwap manual routes

## Changes
- Give the CHEESESwap window a viewport-aware maximum height and an internal vertical scrollbar so it remains reachable when many pools are shown or the page is zoomed.
- Keep the title visible while scrolling the swap contents.
- Reduce spacing, route-row height, token icon size, and control height in the expanded Multiroute area without changing route information or percentages.
- Preserve the larger slider handle and 1% minus/plus controls so adjustments remain easy.
- Keep the window within narrow mobile widths and prevent horizontal overflow.

## Verification
- Check desktop and mobile-sized layouts with several manual routes.
- Confirm scrolling, slider dragging, and 1% controls remain usable.
- Run the existing routing tests and verify the preview build.
