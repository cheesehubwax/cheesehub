# Fix the CHEESEAnal Tombstone heading

- Add the OpenMoji skull asset to the shared emoji map.
- Replace both tombstone symbols in the section heading with the skull.
- Remove the BETA label without changing the section’s data or behavior.
- Verify the heading renders correctly in CHEESEAnal.
